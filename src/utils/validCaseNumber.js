/**
 * Validate the input is a case number
 */
export default function validCaseNumber(casenumber) {
    if (casenumber.length !== 9) {
      return false;
    }
  
    // TODO: add logic to valid case number
    return true;
  }
  